# WikiLense runtime dependencies (Python 3.12). Pinned versions are in requirements.lock.
sentence-transformers>=3.0
torch>=2.2
PyMySQL>=1.1
python-dotenv>=1.0
tqdm>=4.66
numpy>=1.26
fastapi>=0.110
uvicorn>=0.29
